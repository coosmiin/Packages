name: SheetsApi.Client build

on: 
  pull_request:
    branches:
      - master
    paths:
      - 'GoogleApi/src/SheetsApi.Client/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: setup .NET
        uses: actions/setup-dotnet@v1
        with:
          source-url: https://nuget.pkg.github.com/coosmiin/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}        
      - name: dotnet restore
        run: dotnet restore GoogleApi/src/SheetsApi.Client/SheetsApi.Client.csproj
      - name: build
        run: dotnet build GoogleApi/src/SheetsApi.Client/SheetsApi.Client.csproj --configuration Release --no-restore
